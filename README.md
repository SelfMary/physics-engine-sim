# physics-engine

- There are views/world
Basic-world is the basic rigid body physics that uses "Collision.h"
Projectile-world is the projectile specific space whihc uses "Projectile.h" and "Trajectory.h"

Both use the vec3dMath class and PhysicsWorld.

- Projectile includes Projectile specific BodyData
- Trajectory includes use of functions for the body moving in 2d.